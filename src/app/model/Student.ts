export interface toDo { //IStudent
    id: Number;
    task: String;
    check: Boolean;
    // "ID":579,"task":"do angular lab", "check": "true"}
}
export interface toDoAddtion {
    task: string;
    check: Boolean;
}